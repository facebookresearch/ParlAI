export default function valid_utterance(text, search_results, selected_results, isOnboarding) {
  // TODO: add this after refactoring the main code.
  return true;
}

export function valid_search_query(search_query) {
  // TODO: add this after refactoring the main code.
  return true;
}
